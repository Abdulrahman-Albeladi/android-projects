# Project-specific ProGuard rules.
#
# Configure applied rule files through the `proguardFiles` setting in the
# module Gradle build configuration.
#
# WebView JavaScript interfaces, source mappings, and source-file renaming are
# not configured because this project does not demonstrate a requirement for
# them.
